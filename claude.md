# Claude Instructions

Response style rules:

- Do NOT output full code unless explicitly requested
- Avoid code diff outputs
- Avoid bash command outputs
- Focus on key ideas and architecture
- Prefer concise explanations
- If code changes are required, describe the change instead of printing full code
- just tell me which files are changed